# Code Review: nn-attach

- Date: 2026-04-19 00:55:41
- Depth: medium
- Changed files:
  - `packages/cli/src/cli.ts` (mod)
  - `packages/cli/src/commands/worktree/create.ts` (mod)
  - `packages/cli/src/commands/worktree/_shared.ts` (new)
  - `packages/cli/src/commands/worktree/attach.ts` (new)
  - `packages/cli/src/lib/gh.ts` (new)
  - `packages/cli/src/lib/git.ts` (mod)
- Total rounds: 1
- Final status: PASSED

## Round 1

### Findings

| # | File | Line | Severity | Dimension | Description |
|---|------|------|----------|-----------|-------------|
| 1 | packages/cli/src/commands/worktree/attach.ts | 81–82, 114 | MEDIUM | Framework/API | `--pr abc` produced `NaN` from commander's `Number(v)`; `opts.pr !== undefined` still true → `state.pr = NaN`. |
| 2 | packages/cli/src/commands/worktree/attach.ts | 37, 128 | MEDIUM | Consistency | Resume resolved detached-HEAD to the user-typed branch string; `create.ts` resolves to `null`. Would break `showExitPrompt` and later branch deletion. |
| 3 | packages/cli/src/commands/worktree/attach.ts | 85–89 | LOW | Consistency | Plan specifies distinguishing "gh not available" from "no open PR"; implementation conflated them. |
| 4 | packages/cli/src/commands/worktree/attach.ts | 112–115 | LOW | Correctness | On resume with `--pr`, if `readState` returns `null`, update silently skipped with no notice. |
| 5 | packages/cli/src/commands/worktree/attach.ts | 24–25 | LOW | Correctness | `assertValidName` rejects some legit git branch chars (`@`, `+`). Design limitation; `--as` is the escape hatch. |
| 6 | packages/cli/src/lib/gh.ts | 8 | INFO | Framework/API | Verified `$` splits interpolations into single argv tokens → `--jq ${...}` reaches gh as two tokens. OK. |

### Fixes

| # | Issue | Fix |
|---|-------|-----|
| 1 | `--pr` NaN written to state | Tightened commander parser in `cli.ts` to exit non-zero on non-finite / non-positive / non-integer values. |
| 2 | Detached-HEAD fallback uses user branch string | Changed `?? branch` → `?? null` on resume; state writes fall back to the user-typed branch only on fresh create. |
| 3 | gh-missing vs no-PR conflated | `getPrForBranch` now returns a discriminated union (`found`/`none`/`unavailable`); attach prints plan-mandated messages. |
| 4 | Silent skip when state missing on resume `--pr` | Added a `note:` log when `readState` returns null. |

### Skipped

- **#5** (`assertValidName` strictness): intentional — plan says "branch name is never modified"; user can pass `--as` for exotic branches. Not a correctness bug.
- **#6**: informational only.

## Summary

- Total findings: 6 (HIGH: 0, MEDIUM: 2, LOW: 3, INFO: 1)
- Fixed: 4
- Remaining: 2 (LOW #5 is by design; INFO #6 requires no action)
- Typecheck + build both pass after fixes.
